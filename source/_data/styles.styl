// Custom styles.
// 整体背景设置
body {
 	  background:url(/images/RbtPqji.png);// 设定背景图片,images同处于source文件夹下
 	  background-repeat: no-repeat;// 设定背景图片非重复填充
    background-attachment:fixed;// 设置背景图片不随页面滚动
    background-position:50% 50%;// 设置背景图片位置
	background-size: cover;// 设置保持图像的纵横比并将图像缩放成将完全覆盖背景定位区域的最小大小
    color:#000
}


.content-wrap {
  opacity: 0.8;
}

.sidebar {
  background: rgba(255,255,255,0.5) none repeat scroll !important;
}

.header-inner {
  background: rgba(255,255,255,0.5);
}

//.popup {  //搜索框透明
//  opacity: 0.6;
//}

.post-block {
	background: rgba(255,255,255,0.5) none repeat scroll !important;
    color:
}
.sidebar-inner {
  //background: rgba(255,255,255,0.3) none repeat scroll !important;
  background: rgba(255,255,255,0.3)
  color:#000
  }

  	// 主页文章添加阴影效果
.post {
   margin-top: 60px;
   padding: 20px;
   margin-bottom: 60px;
   -webkit-box-shadow: 0 0 5px rgba(120, 128, 114, 1.5);
   -moz-box-shadow: 0 0 5px rgba(202, 203, 204, .5);
}







